package half_adder_pkg;
  // Constants for timing, if needed.  Could be parameters instead.
  localparam real INVERT_DELAY = 5.0;  // ns
  localparam real AND_DELAY    = 10.0; // ns
  localparam real OR_DELAY     = 10.0; // ns
  localparam real NAND_DELAY   = 10.0;
endpackage : half_adder_pkg
